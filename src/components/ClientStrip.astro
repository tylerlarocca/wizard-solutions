---
type Client = {
  name: string;
  logo: string;
  href?: string;
};

const clients: Client[] = [
  {
    name: "Outlier",
    logo: "/clients/Outlier.svg",
    href: "https://getoutlier.com",
  },
  {
    name: "Backdraft",
    logo: "/clients/BackDraft.png",
    href: "https://backdraftsuppressors.com",
  },
  {
    name: "Witt Machine",
    logo: "/clients/WittMachine.svg",
    href: "https://wittmachine.net",
  },
  {
    name: "HealCorp",
    logo: "/clients/HealCorp.svg",
    href: "https://www.heal-corp.org/",
  },
  {
    name: "Helping a Hero",
    logo: "/clients/HelpingHero.svg",
    href: "https://helpingahero.org/",
  },
];
---

<div class="rounded-2xl border border-white/10 bg-white/5 p-6">
  <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">
    {clients.map((c) => (
      <div class="flex items-center justify-center rounded-xl border border-white/10 bg-zinc-900 px-5 py-5">
        {c.href ? (
          <a href={c.href} target="_blank" rel="noopener noreferrer">
            <img
              src={c.logo}
              alt={`${c.name} logo`}
              class="h-14 sm:h-16 lg:h-20 w-auto opacity-80 saturate-75 transition duration-300 hover:opacity-100 hover:saturate-150"
              loading="lazy"
            />
          </a>
        ) : (
          <img
            src={c.logo}
            alt={`${c.name} logo`}
            class="h-14 sm:h-16 lg:h-20 w-auto opacity-80 saturate-75 transition duration-300 hover:opacity-100 hover:saturate-150"
            loading="lazy"
          />
        )}
      </div>
    ))}
  </div>

  <p class="mt-4 text-xs text-white/50">
    Trusted by teams across software, e-commerce, and media.
  </p>
</div>
